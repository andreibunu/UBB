package com.first;

public class STHashTable {
    private SLNode[] table;
    private int size;

    public STHashTable(int size){
        table = new SLNode[];
        size = 0;
    }

    public boolean isPrime(int number){
        if(number)
    }
}
